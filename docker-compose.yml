version: '2'

services:
        container_01:
                build:
                        context: .
                        dockerfile: Dockerfile-1
                ports:
                 - "8888:8888"
                volumes:
                 - ./volume:/var/volume
        container_02:
                build:
                        context: .
                        dockerfile: Dockerfile-2
                ports:
                 - "5000:5000"
                volumes:
                 - ./volume:/var/volume
        container_03:
                build:
                        context: .
                        dockerfile: Dockerfile-3
                ports:
                 - "3000:3000"
                volumes:
                 - ./volume:/home/project

